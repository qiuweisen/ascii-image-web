# ASCII Image 搜索意图与页面策略研究

**版本:** 0.1（初步研究）  
**日期:** 2026-08-10  
**用途:** 指导首页与首批长尾页面设计，不替代上线后的 GSC 数据

## 1. 研究结论

当前最稳妥的产品路线是：

1. 用首页建立完整的 `image to ascii` 产品底座和主题权威。
2. 优先上线 `/line-art/`，用低复杂度、低竞争工具验证收录和转化。
3. 再根据真实搜索数据选择 `/braille/`、`/color/`、`/dots/` 的顺序。
4. 所有风格页复用同一转换核心，但必须提供真实不同的输出行为，不能只换标题。

## 2. 意图分层

| 意图层 | 代表查询 | 用户想做什么 | 页面回应 |
|---|---|---|---|
| 核心工具 | `image to ascii`, `image to ascii art` | 上传图片并得到字符画 | 首页直接提供转换器 |
| 输出风格 | `image to ascii color`, `image to ascii dots`, `image to ascii braille` | 生成特定字符风格 | 独立风格工具页 |
| 单行作品 | `line ascii art`, `single line ascii art` | 浏览、筛选并复制现成单行字符画 | `/line-art/` 分类作品库 |
| 通用生成 | `ascii generator`, `ascii art generator` | 探索或生成不同 ASCII 内容 | 后续扩展页，不作为 MVP 主入口 |
| 内容复制 | `thumbs up ascii`, `heart ascii` | 找可复制的现成符号 | 第三阶段 `/symbols/` 内容页 |

### 意图判断

`image to ascii` 的默认回应必须是“立即上传并生成”，而不是教程或画廊。教程、
FAQ 和示例用于消除疑虑、支撑 SEO，不应挤出首屏工具。

`line ascii art` 与图片转换是不同任务。Yahoo/Bing 当前结果以分类作品库和一键复制为主，
并非文字转图案生成器。因此它应是独立的可索引作品库，而不是首页工具的隐藏模式；
文本生成可作为后续增强，但不应成为首版主流程。

## 3. 关键词与页面决策

| 页面 | 主词 | 初步竞争判断 | 首屏必须出现 |
|---|---|---|---|
| `/` | `image to ascii` | 中等，需产品和站点权重 | 上传区、原图/ASCII 示例、输出预览 |
| `/line-art/` | `line ascii art` | 低，适合快速验证 | 分类、可复制作品、搜索/筛选 |
| `/braille/` | `image to ascii braille` | 初步低，需验证搜索量 | 图片上传、盲文输出、字符密度说明 |
| `/color/` | `image to ascii color` | 初步低到中 | 图片上传、颜色输出、终端/HTML 导出说明 |
| `/dots/` | `image to ascii dots` | 初步低到中 | 图片上传、点阵输出、密度控制 |

需求分析中的 KD、搜索量和竞品排名是研究假设，不作为承诺。上线前复核：

- Google 和 Bing 的国家/语言一致的真实 SERP；
- 前 10 名页面类型（工具、教程、画廊或视频）；
- People Also Ask 与相关搜索；
- 页面是否 SSR、首屏是否直接可用、是否支持移动端；
- GSC 实际展现和查询词，至少观察 4 周。

## 4. 页面设计决策

### 4.1 首页

```text
导航
  ↓
原图 ↔ ASCII 对比舞台
  ↓
H1 + 本地处理说明 + 开始转换 CTA
  ↓
转换工作区
  ├─ 左：上传和参数
  └─ 右：可滚动 ASCII 输出、复制、下载
  ↓
使用场景 / 参数教程
  ↓
风格入口（line-art / braille / color / dots）
  ↓
FAQ / 隐私 / 页脚
```

设计重点：用户第一次进入页面时，在不阅读长文的情况下就能完成一次生成；
任何营销文案都必须服务于“上传 → 生成 → 复制/下载”。

### 4.2 `/line-art/`

```text
H1 + 简短用途说明
  ↓
搜索/分类筛选
  ↓
可复制单行作品列表
  ├─ 动物 / 表情 / 爱心 / 武器等分类
  └─ 单项预览 + 一键复制
  ↓
使用说明 / FAQ
```

该页不应要求注册、不应先展示广告，也不应把图片上传作为主流程。作品正文必须存在于
SSR HTML 中，便于搜索引擎理解和用户在脚本失败时仍能复制。

### 4.3 风格页模板

风格页共享上传和输出基础组件，但每页必须说明：

- 该风格解决什么问题；
- 字符如何表达明暗或颜色；
- 适合复制到哪里；
- 为什么该页面的输出不同于经典 ASCII。

## 5. 用户问题与产品回答

| 用户疑问 | 页面回答 | 产品功能 |
|---|---|---|
| 图片会被上传吗？ | 默认在浏览器本地处理 | 本地转换，不保存原图 |
| 为什么人物变形？ | 字符有不同宽高比 | 宽高比补偿开关和说明 |
| 如何更清晰？ | 增加列数、调整对比度或换字符集 | 宽度、亮度、对比度、字符集 |
| 能复制到终端吗？ | 输出保持等宽和换行 | 纯文本复制、TXT 下载 |
| 手机上能用吗？ | 工具支持触控和滚动 | 单列布局、稳定输出区、移动复制 |
| 出错怎么办？ | 明确说明原因和下一步 | 格式/大小/解码错误状态 |

## 6. 需要验证的假设

### 高优先级

- 用户更常复制纯文本，还是下载 PNG。
- 用户首选经典黑白，还是直接寻找彩色/点阵风格。
- 移动端上传比例和复制成功率是否显著低于桌面端。
- `/line-art/` 的搜索流量是否真的转化为复制行为。

### 中优先级

- 自定义字符集是否值得占用控制面板空间。
- 用户是否需要裁剪和尺寸预设。
- 作品分享链接是否比下载更有传播性。

### 暂不验证

- 视频、摄像头、API 和 AI 功能，除非 MVP 数据显示明确需求。

## 7. 研究与验证计划

### 上线前人工研究

对每个目标词记录前 10 个结果：页面类型、Title/H1、首屏 CTA、输入方式、导出能力、
广告情况、移动端体验和索引状态。截图或链接保存到研究记录，避免只记主观印象。

### 上线后数据验证

1. GSC：查询词、页面、国家、设备、展现、点击和平均排名。
2. 产品分析：上传开始、生成成功、复制、下载、风格页跳转和错误原因。
3. 每两周检查一次，四周后决定下一个风格页。

### 决策门槛

- 页面已收录但无点击：优先改 Title、描述、首屏解释和内部链接。
- 有点击但无上传：改首屏工具可见性、CTA 和隐私说明。
- 有生成但无复制/下载：改输出可读性、按钮层级和导出格式。
- 某风格页有稳定展现和使用：优先扩展该风格，而不是盲目增加新页面。

## 8. 当前执行顺序

1. 按 [`PRD.md`](PRD.md) 完成首页转换核心和 `/line-art/`。
2. 用 `DESIGN.md` 统一两页的视觉和交互 token。
3. 上线后接入 GSC 和核心事件埋点。
4. 用真实数据决定 `/braille/`、`/color/`、`/dots/` 的开发顺序。

## 9. 浏览器实地观察（2026-08-10）

### `imagetoascii.app`

直接页面观察确认了以下模式：

- 首屏是原图/ASCII 对比滑块，随后马上进入上传工具。
- 明确强调“浏览器本地处理、无需注册、图片不上传”。
- 工具同时提供图片上传和 AI 生成两种入口；AI 不是默认路径。
- 内容覆盖社交头像、壁纸、海报、周边、README/终端和封面等使用场景。
- 教程解释字符明暗映射、字符集、反色、颜色开关、宽度和导出方式。
- 页面使用大量 FAQ 和可读正文支撑搜索主题，而不是只放一个交互组件。

**对我们的影响:** 首页必须把隐私、输出用途和“立即可用”放在工具附近；至少要有
使用场景和“如何工作”内容。AI 生成可以保留为后续入口，不应进入 MVP 主流程。

### `image2ascii.com`

直接页面观察确认了以下模式：

- 提供中文、英语、韩语、日语、西班牙语和德语切换。
- 首页把图片转换和视频转换作为两个清晰入口。
- 文案强调浏览器处理和不上传服务器。
- 首页展示大量 ASCII 结果，并用三步流程解释“上传 → 调整 → 导出”。
- 导出承诺包括 TXT、PNG 和 WebM；视频是独立的产品路径。

**对我们的影响:** PNG 导出不能被永久排除在产品路线外，至少应在 MVP 之后进入验证；
多语言适合在英文产品稳定后扩展，不应为了追赶竞品而提前增加复杂度。

### `ascii-magic.com`

直接页面观察确认了以下模式：

- 以“一个工具、多种风格”为核心，首页提供 Characters、Dither、Dots、Pixel Art、
  Mosaic、LEGO、Animated ASCII 等多个入口。
- 每种风格有独立的可索引页面和示例，风格页能直接打开编辑器并加载示例图。
- 通过 Community、Recipes、Usecases、Blog、Compare 形成内容和内部链接网络。
- FAQ 覆盖工具能力、隐私、移动端、速度、导出和商业使用。
- 首页首屏仍然保持对比滑块和“打开工具”CTA，没有被内容目录取代。

**对我们的影响:** 长期应采用“一个转换核心 + 多个真正不同的风格页”架构；
但 MVP 只做经典 ASCII 和 `/line-art/`，避免一开始复制 13 种风格的复杂度。

### SERP 访问限制

本次浏览器访问 Google 和 DuckDuckGo 时触发了人机验证，因此没有把当前结果页的排名、
PAA 或相关搜索当作已验证证据。关键词难度和搜索量仍需在可用的人工浏览器环境、关键词
工具或上线后的 GSC 中复核。

## 10. 设计落地新增决策

1. 首页首屏保留对比舞台，但工具 CTA 必须在首屏或紧接其后可见。
2. 输出区除纯文本复制外，组件接口预留 PNG 导出，不在 MVP 阶段阻塞上线。
3. “风格”作为可扩展导航模型设计；首期只展示经典 ASCII 和单行 ASCII。
4. 使用场景内容优先写 README/终端、社交头像和海报三类，覆盖文本与视觉两种动机。
5. FAQ 必须包含本地处理、输出变形、字符集、复制、导出和商业使用问题。

## 11. Yahoo/Bing SERP 实证（2026-08-10）

本节来自可见 Chrome 中的 Yahoo Search（结果由 Bing 提供），地区页面为英文结果。
它只代表本次 Yahoo/Bing 快照，不等同于 Google 排名或关键词搜索量。

### 核心词 `image to ascii`

首屏结果包括 `asciiart.eu/image-to-ascii`、ConvertICO、EZASCII、
`generator-ascii.art`、MiniWebtool、Jasper Bernaers 和 `imagetoascii.app`。
结果摘要反复出现以下能力：实时预览、自定义字符集、宽度/亮度/对比度/反色、浏览器
本地处理、并排对比、剪贴板输入，以及 PNG/JPEG/SVG/TXT 导出。

相关搜索包括 `image to ascii art`、`image to ascii generator`、
`image to ascii converter`、`image to ascii text`、`image to braille`、
`image to ascii color` 和 `image to ascii dots`。

**产品判断：** 上传后立即预览和本地处理属于入场能力；剪贴板粘贴是低成本、高频输入，
应进入 MVP。多格式视觉导出是明显竞争点，但首版先交付 TXT，PNG/SVG 按真实导出需求排序。

### `line ascii art`

前列结果是 ASCII Art Archive、asciii.com、1lineart.kulaone.com 和 Textartify，
页面标题和摘要都强调 `one line`、分类作品、聊天/简介场景和一键复制。相关搜索包括
`one line ascii art`、`single line ascii art`、`simple one line ascii art` 和
`one line ascii art generator`。

**产品判断：** 主导意图是找现成作品，不是输入文本生成图案。`/line-art/` 首版应提供
服务端可索引的分类作品库、搜索/筛选和单项复制；生成器降为后续实验。

### `image to ascii braille`

前列结果包含 LAZE SOFTWARE、OnlinePhotoTools、a.tools、Fontbag、多个 GitHub Pages
工具和 Asciinova。摘要一致把 Braille 描述为 2x4 点阵 Unicode 字符，并强调更高细节、
阈值/抖动、反色、宽度、复制以及 PNG/SVG/TXT 导出。

**产品判断：** 这是明确、独立的转换意图，不应只给经典 ASCII 换字符集标题。
实现时需要真实 2x4 像素映射和可选 dithering，并解释它是视觉点阵艺术而非盲文翻译。

### `image to ascii color` 与 `image to ascii dots`

Color 结果以通用转换器和独立 color 页面混合为主，摘要强调保留每个字符的源图颜色、
调色、字体和视觉格式导出。Dots 结果更混合，既有通用转换器，也有 Braille/Dots 模式
和专用 dot-art 工具；相关搜索仍以 converter、generator、copy 和 translator 为主。

**产品判断：** Color 可作为共享转换核心中的真实渲染模式，并形成独立落地页；Dots
需要先定义与 Braille 的差异，否则容易产生内容和功能重复。当前仍维持 Braille 优先于
Color/Dots 的第二阶段顺序。

### 新增竞品交互核对

- **EZASCII：** 9 套字符集、字体和图像参数、彩色模式、实时预览、本地处理，支持
  PNG/JPEG/SVG/TXT。其内容明确提醒复制结果需要等宽字体。
- **generator-ascii.art：** 面向设计师，提供字符预设/自定义、字体导入、色彩、cell ratio
  和 PNG/JPG/WebP/SVG/TXT 导出，证明“设计资产导出”是独立使用场景。
- **Jasper Bernaers：** 上传、粘贴和 demo 三个低摩擦入口；Classic、Detailed、Dots、
  Blocks、Neon、Matrix 等一键预设；支持 Discord 专用复制、分享、TXT 和 PNG。

### 对执行顺序的最终影响

1. 首页 MVP 增加从剪贴板粘贴图片，保留 JPG/PNG/WebP 的明确支持范围。
2. `/line-art/` 改为作品库产品，不在首版承诺文本生成。
3. `/braille/` 仍是图片风格页中的第一候选；实现需包含 2x4 映射和 dithering。
4. PNG 作为 Phase 1 的首个视觉导出实验，SVG 随设计师/打印需求验证；GIF/BMP 仅做
   浏览器可解码兼容性探索，不写入 MVP 验收承诺。
