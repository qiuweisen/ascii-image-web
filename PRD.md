# ASCII Image 产品需求文档

**版本:** 1.0  
**日期:** 2026-08-10  
**产品代号:** ASCII Image  
**域名:** `asciiimage.com`  
**状态:** MVP 需求基线

## 1. 产品结论

ASCII Image 是一个浏览器端图片转 ASCII 工具站。用户上传图片，在本地完成
字符化处理，并立即复制或下载结果。首页长期围绕 `image to ascii` 建设；
首批低竞争子页面用于验证搜索需求、积累自然流量和站点权重。

产品不以 EMD 作为排名前提。当前策略是用一个短、可记忆、语义清晰的品牌域名，
依靠 SSR 内容、主题聚焦、真实工具体验和持续外链获得排名。

## 2. 背景与机会

需求分析中的竞品观察显示：

- `image to ascii` 是明确的图片转字符意图，搜索量和趋势值得验证。
- 新站在该赛道已有排名案例，说明域名权重不是唯一门槛。
- 多数竞品的 SEO、首屏文案或可访问性不完整，产品体验存在超越空间。
- `image to ascii color`、`image to ascii dots`、`image to ascii braille`、
  `line ascii art` 等长尾词竞争较低，适合先做小页面验证。

报告中的 KD、搜索量、流量和收入均属于决策假设。上线前必须使用 GSC、关键词工具
和真实广告数据复核，不以单次第三方估算作为承诺。

## 3. 目标与非目标

### 3.1 MVP 目标

1. 用户无需注册即可完成一次图片转 ASCII。
2. 图片默认只在浏览器处理，不上传到服务器。
3. 用户能控制输出宽度、字符集、对比度/亮度等关键参数。
4. 用户能复制纯文本并下载 `.txt` 文件。
5. 首页具备可抓取的 SSR 内容、FAQ 和内部链接，明确服务 `image to ascii`。
6. 上线 `/line-art/` 作为第一个低竞争、可复制的单行作品库页面。
7. 在桌面端和移动端均保持稳定布局、键盘可用和清晰状态反馈。

### 3.2 MVP 非目标

- 视频转 ASCII、摄像头实时流、WebM 导出。
- AI 图片描述或 AI 生成 ASCII。
- 账号体系、云端作品保存、协作和社交功能。
- 公开 API、计费系统和复杂的图片编辑器。
- 大规模符号图鉴内容生产。

这些方向进入后续阶段，必须以 MVP 的使用数据和搜索验证结果为依据。

## 4. 用户与核心场景

### 4.1 目标用户

| 用户 | 需求 | 成功结果 |
|---|---|---|
| 好奇的普通用户 | 快速把照片变成字符画 | 30 秒内看到可复制结果 |
| 社交内容创作者 | 制作头像、海报或帖子素材 | 能调整风格并导出文本 |
| 开发者/终端用户 | 获得等宽、可复用的字符输出 | 输出格式稳定，复制不丢空格 |
| 搜索长尾用户 | 找彩色、点阵、盲文或单行工具 | 进入对应页面即可直接操作 |

### 4.2 主用户旅程

1. 从搜索结果进入首页或风格子页。
2. 首屏看到示例对比和“本地处理”承诺。
3. 拖放图片或选择文件。
4. 工具生成默认结果，用户调整参数。
5. 复制或下载结果。
6. 通过结果区推荐进入其他风格页面，或重新开始。

异常路径：格式不支持、文件超过 10MB、图片解码失败、输出过宽导致阅读困难时，
必须在工具内显示可理解的错误和恢复动作。

## 5. 信息架构与页面策略

```text
/
├── 首页：Image to ASCII Converter
│   ├── 原图/ASCII 对比示例
│   ├── 图片转换工作区
│   ├── 使用场景、教程、FAQ
│   └── 入口：line-art / color / dots / braille
├── /line-art/
│   └── 单行 ASCII Art Generator + 示例画廊
├── /color/
│   └── 彩色 ASCII 生成器（第二阶段）
├── /dots/
│   └── 点阵 ASCII 生成器（第二阶段）
├── /braille/
│   └── Braille 字符生成器（第二阶段）
└── /symbols/（第三阶段）
    └── 可复制符号和表情长尾内容
```

### 5.1 首页 SEO 与内容结构

- **Title:** `Image to ASCII - Convert Images to ASCII Art Free`
- **H1:** `Image to ASCII Art Converter`
- 首屏必须包含可工作的转换器或明确的立即进入按钮。
- 以自然语言解释：支持格式、处理位置、输出用途和隐私边界。
- 页面下半部提供教程、使用场景、FAQ 和风格入口，避免只放工具没有可读内容。
- 每个子页面必须有独立 Title、H1、说明文字、FAQ 和 canonical/内部链接。

## 6. 功能需求

### 6.1 图片输入

| 编号 | 需求 | 验收标准 |
|---|---|---|
| FR-01 | 支持点击选择图片 | 接受 JPG、PNG、WebP；选择后显示文件名和状态 |
| FR-02 | 支持拖放 | 拖入有效文件时有视觉反馈，松开后自动开始处理 |
| FR-02A | 支持粘贴图片 | 聚焦页面后粘贴剪贴板图片可直接处理，并显示来源状态 |
| FR-03 | 校验文件 | 超过 10MB 或格式不支持时阻止处理并显示原因 |
| FR-04 | 示例图片 | 首次访问有内置示例，用户无需准备文件即可体验 |
| FR-05 | 隐私提示 | 上传区附近明确说明图片默认不离开设备 |

### 6.2 转换参数

MVP 必须支持：

- 输出宽度：建议范围 40-180 列，步进 5；显示实时列数。
- 字符集：至少提供明暗字符集和自定义字符集入口。
- 亮度/对比度：用 range 控件调整，并在输出更新后保留当前值。
- 反色：二元开关。
- 宽高比补偿：默认开启，避免人物和物体被压扁。
- 输出模式：经典 ASCII；子页可复用同一转换核心。

参数改变后应在 150ms 内开始反馈；大图处理时显示 `processing` 状态，避免用户
误以为界面失效。

### 6.3 输出与导出

| 编号 | 需求 | 验收标准 |
|---|---|---|
| FR-06 | 输出预览 | 使用等宽字体、保留空格和换行，可滚动查看完整结果 |
| FR-07 | 复制 | 一键复制纯文本；成功后按钮或状态文本反馈，不依赖 toast |
| FR-08 | 下载 | 下载 UTF-8 `.txt`，文件名包含 `ascii-art` 和时间或原文件名 |
| FR-09 | 状态 | 显示 ready、processing、complete、error 四类状态 |
| FR-10 | 重置 | 用户可恢复默认参数并重新使用示例或选择新文件 |

### 6.4 单行 ASCII 页面（MVP 子页）

- 路径：`/line-art/`。
- 提供可搜索、可筛选的内置作品分类（动物、人物、表情、爱心、物品等）。
- 每项必须是一行或短行字符画，可单独复制；不依赖图片上传或前端生成。
- 作品文本和分类链接必须由 SSR 输出，脚本失败时仍可阅读和手动复制。
- 首版不承诺“输入任意文本生成单行图案”；根据搜索和复制数据再验证生成器。
- 页面目标词：`line ascii art`；次要词：`single line ascii art`、
  `ascii art one line`（上线前复核数据）。

### 6.5 内容与 FAQ

首期至少覆盖：

- 什么是 ASCII art？
- 如何获得更清晰的图片转 ASCII 结果？
- 为什么输出看起来被拉伸？
- 图片会上传到服务器吗？
- 支持哪些图片格式和大小？
- 如何在终端、Discord 或社交媒体中使用？

FAQ 必须回答真实操作问题，不能堆砌关键词。

## 7. 设计与交互要求

所有视觉实现遵循 [`DESIGN.md`](DESIGN.md)：

- 深色终端工作台、荧光字符前景、青色细线边界。
- 等宽字体、4px 控件圆角、1px 边界和虚线分隔。
- 首页第一视觉使用“原图 ↔ ASCII”拖拽对比。
- 工具区采用控制面板 + 输出终端的双栏结构，`920px` 以下改为单列。
- 控件最小高度 44px；主 CTA 48px；提供清晰 hover、active、focus 和 disabled 状态。
- 扫描线、字符雨和 ticker 只能作为辅助，不得遮挡输出或影响阅读。
- `prefers-reduced-motion` 下移除非必要动效。

## 8. 技术与数据要求

### 8.1 运行边界

- 继续使用 TanStack Start + React 19 + Cloudflare Workers 架构。
- 图片转换核心运行在浏览器 Canvas/纯前端逻辑，不调用 Node.js API。
- 页面正文和 SEO 元数据由 SSR 输出；交互工具在 hydration 后接管。
- 不上传原图，不在 MVP 保存用户图片或输出内容。

### 8.2 性能预算

- 首屏 HTML 可直接读取标题、说明、FAQ 和内部链接。
- 首次可交互目标：中端移动设备 3 秒内（以真实监控复核）。
- 大图处理不阻塞主线程；必要时使用分块处理或 Web Worker。
- 结果区采用稳定高度和滚动容器，避免输出变化造成页面跳动。

### 8.3 可访问性

- 上传、滑块、tab、开关和复制/下载操作全部可键盘操作。
- 对比滑块使用 `role="slider"`、键盘箭头调整和 `aria-valuenow`。
- 输出更新放入 `aria-live` 区域；错误同时使用文本和状态属性。
- 颜色不能作为唯一状态信号；焦点轮廓必须清晰可见。

## 9. SEO 与增长计划

### 9.1 页面优先级

1. 首页：`image to ascii`，先完成产品和内容基础，不等待外链才上线。
2. `/line-art/`：低成本验证工具页和长尾流量。
3. `/braille/`、`/color/`、`/dots/`：复用转换核心，分别承接风格词。
4. `/symbols/`：有稳定生成和索引能力后再扩展符号内容。
5. `ascii generator`、`ascii art generator`：整站有权重后再重点投入。

### 9.2 内容质量要求

- 每个工具页必须有独特的工具行为，不仅是换标题的模板页。
- 用真实生成示例、输入限制、参数解释和复制结果支撑页面主题。
- 添加 Organization、WebApplication、FAQPage 等结构化数据前先确认内容真实对应。
- 通过 sitemap、canonical、语言标签和内部链接保持可抓取性。

### 9.3 外链节奏

- 首两个月优先产品、索引和数据验证，不购买大量低质量链接。
- 通过竞品外链、开发者社区、ASCII 展示作品和教程获得相关引用。
- 以 GSC 展现、点击、收录和目标词排名决定下一轮投入，而不是固定承诺某个排名日期。

## 10. 变现设计

### MVP

- 工具保持免费。
- 预留不打断转换流程的广告位；广告必须避开上传区和输出区。
- 不在首屏弹出注册、订阅或全屏广告。

### 后续

- 去广告。
- 高清或大尺寸导出、批量处理。
- API 订阅，面向开发者提供额度和稳定输出格式。
- 公共作品画廊，用户授权后生成可索引作品页。

变现判断以真实 RPM、回访率、导出使用率和 API 询价为依据。这个项目的合理定位是
低成本 SEO 工具站和可复用产品能力，不把月收入估算当作 MVP 成功标准。

## 11. 分阶段路线图

### Phase 0：基础上线

- 首页 SSR 内容、转换核心、上传/拖放、默认示例。
- 宽度、字符集、亮度/对比度、反色、宽高比补偿。
- 复制、TXT 下载、错误和移动端布局。
- `/line-art/` 工具页、FAQ、基础 SEO、sitemap、隐私说明。

### Phase 1：长尾验证（上线后 1-3 个月）

- 接入 GSC 和产品事件分析。
- 根据查询和使用率确定 `/braille/`、`/color/`、`/dots/` 的顺序。
- 增加真实示例、教程和相关内部链接。
- 优化大图性能、结果可读性和移动端复制体验。
- 根据导出事件优先验证 PNG，设计师/打印需求成立后再增加 SVG。

### Phase 2：商业化验证（3-6 个月）

- 小流量广告实验和去广告权益。
- 高清导出或批量处理实验。
- 收集 API 等候名单，不先实现完整 API。

### Phase 3：扩张

- 视频/摄像头、公共作品页、API 和更多语言，只有在数据证明需求成立后进入排期。

## 12. 埋点与成功指标

### 核心事件

- `page_view`
- `upload_started`
- `upload_rejected`（含原因）
- `conversion_started`
- `conversion_completed`
- `parameter_changed`（不记录图片内容）
- `copy_clicked`
- `download_clicked`
- `style_navigation_clicked`
- `line_art_generated`

### MVP 指标

| 指标 | 目标方向 |
|---|---|
| 首次访问到首次生成转化率 | 持续提升 |
| 生成成功率 | > 98%（排除用户格式错误） |
| 生成到复制/下载率 | 持续提升 |
| 移动端生成成功率 | 不低于桌面端 5 个百分点 |
| 长尾页收录率 | 上线后 4 周可验证 |
| 首页与子页自然点击 | 按月增长 |

不设置未经验证的绝对流量承诺；每两周根据 GSC 和事件数据调整优先级。

## 13. 验收清单

- [ ] 首页标题、H1、描述和正文明确对应 `image to ascii`。
- [ ] 无文件时可用内置示例完成完整旅程。
- [ ] JPG、PNG、WebP 成功处理；超限和损坏文件有恢复提示。
- [ ] 文件选择、拖放和剪贴板粘贴三种输入路径均可生成结果。
- [ ] 参数调整后输出正确，空格、换行和等宽显示不丢失。
- [ ] 复制与 TXT 下载在桌面和移动端均可用。
- [ ] `/line-art/` 可独立访问、筛选和复制作品，作品正文可被 SSR 抓取。
- [ ] 所有关键控件有键盘焦点、aria 名称和状态反馈。
- [ ] `prefers-reduced-motion` 已验证。
- [ ] 生产构建、相关 Playwright E2E 和移动端手工流程通过。
- [ ] GSC、robots、sitemap、canonical 和分析事件已检查。
